<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Camera-roll import needs no permission on Android 10+ (API 29): Capacitor
      Camera 8's chooseFromGallery goes through the system Photo Picker
      (ActivityResultContracts.PickVisualMedia), which hands back a single URI
      the user chose and grants nothing else. READ_MEDIA_IMAGES is therefore
      NOT required, despite being the usual answer for API 33+.

      Android 9 and below is the exception. minSdkVersion here is 24, so those
      devices are in scope, and the plugin's own logic (IonCameraFlow, the
      SDK_INT < Q branch) requires the legacy storage permissions there: for
      chooseFromGallery always, and for takePhoto whenever saveToGallery is
      true, which is what our capture path already passes.

      Capped at maxSdkVersion 28 so modern installs never see a broad storage
      permission, and Play's listing doesn't advertise one. Both READ and WRITE
      are declared because the plugin's `saveGallery` alias covers the pair and
      its isPermissionDeclared check is per-alias.
    -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
</manifest>
