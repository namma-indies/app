source "https://rubygems.org"
gem "fastlane"
# Pinned: the openssl gem bundled with Homebrew's Ruby 4.0.6 hits "invalid
# curve name" parsing Apple's PKCS8-wrapped EC key (app_store_connect_api_key)
# -- a known mismatch between the default gem and the linked OpenSSL 3.x lib.
gem "openssl", ">= 3.2"
